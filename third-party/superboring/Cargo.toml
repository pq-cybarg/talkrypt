# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "superboring"
version = "0.1.11"
authors = ["Frank Denis <github@pureftpd.org>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "A reimplementation of the 'boring' crate in pure Rust."
homepage = "https://github.com/jedisct1/rust-superboring"
readme = "README.md"
keywords = [
    "boringssl",
    "crypto",
    "cryptography",
    "boring",
    "rust",
]
categories = [
    "wasm",
    "api-bindings",
    "cryptography",
]
license = "ISC"
repository = "https://github.com/jedisct1/rust-superboring"

[features]
default = ["enabled"]
enabled = [
    "rrsa",
    "rand",
    "hmac-sha256",
    "hmac-sha512",
    "aes-keywrap",
    "aes-gcm",
    "ml-dsa",
]

[lib]
name = "superboring"
path = "src/lib.rs"

[dependencies.aes-gcm]
version = "0.10"
optional = true

[dependencies.aes-keywrap]
version = "0.9.0"
optional = true

[dependencies.hmac-sha256]
version = "1.1.13"
features = ["traits010"]
optional = true

[dependencies.hmac-sha512]
version = "1.1.11"
features = [
    "traits010",
    "sha384",
]
optional = true

# talkrypt force-widen (2026-06-20): "^0.1" (was "=0.1.0-rc.11"). superboring
# 0.1.11's mldsa code already targets the released ml-dsa API
# (SigningKey::<P>::from_seed, to_seed, signature::Keypair::verifying_key,
# sign_deterministic) — all present in 0.1.1 — so the exact rc.11 pin is merely
# conservative. talkrypt's own ML-DSA-87 identity uses ml-dsa 0.1.1, and Cargo
# won't carry both rc.11 and 0.1.1 (same 0.1.x compatibility range). Widening to
# ^0.1 unifies superboring onto 0.1.1, preserving nym's PQ-JWT path with NO
# downgrade of our crypto and NO source edits. Applied via [patch.crates-io] at
# the workspace root; source otherwise verbatim superboring 0.1.11.
[dependencies.ml-dsa]
version = "^0.1"
optional = true

[dependencies.rand]
version = "0.8.5"
optional = true

[dependencies.rrsa]
version = "0.9.10"
features = [
    "getrandom",
    "hazmat",
    "pem",
    "sha2",
    "std",
    "u64_digit",
]
optional = true
package = "rsa"

[target.'cfg(all(any(target_arch = "wasm32", target_arch = "wasm64"), target_os = "unknown"))'.dependencies.getrandom]
version = "0.2"
features = ["js"]
