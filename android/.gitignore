# Build outputs and machine-local config
.gradle/
build/
app/build/
local.properties

# Derived artifacts regenerated by build-apk.sh (never committed):
#  - the FFI shared library (cargo ndk)
#  - the uniffi-generated Kotlin bindings (uniffi-bindgen)
app/src/main/jniLibs/
app/src/main/kotlin/uniffi/
